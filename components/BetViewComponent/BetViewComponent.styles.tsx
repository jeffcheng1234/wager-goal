import { StyleSheet } from "react-native";

export const BetViewComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "flex-start",
    backgroundColor: "#ffffff",
  },
  right: {
    marginLeft: 20,
    width: "100%",
  },
  bottomRow: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "space-between"
  }
});
