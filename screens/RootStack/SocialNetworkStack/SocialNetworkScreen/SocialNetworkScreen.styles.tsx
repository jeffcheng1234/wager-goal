import { StyleSheet } from "react-native";

export const SocialNetworkScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'flex-start',
    backgroundColor: "#ffffff",
  },
  right: {
      marginLeft: 20,
      width: "100%"
  },
});
